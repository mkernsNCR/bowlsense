{
  "name": "bowlsense-sites",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend run build && node scripts/build-sites.mjs",
    "test": "npm run build && node scripts/test-sites-worker.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
